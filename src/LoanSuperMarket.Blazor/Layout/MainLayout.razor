@inherits LayoutComponentBase
@inject NavigationManager NavigationManager
@inject AuthApiClient AuthApiClient
@inject LoanHubClient LoanHubClient
@inject JwtAuthenticationStateProvider AuthProvider

<div class="page-shell">
    <div class="flex min-h-screen">
        <aside class="hidden lg:flex lg:w-72 lg:flex-col bg-[#071A2F] text-white">
            <div class="flex h-20 items-center px-6 border-b border-white/10">
                <div>
                    <div class="text-xl font-bold tracking-tight">
                        Loan Super Market
                    </div>
                    <div class="text-xs text-blue-200 mt-1">
                        Blazor Enterprise Platform
                    </div>
                </div>
            </div>

            <nav class="flex-1 px-4 py-6 space-y-1 overflow-y-auto">
                @* Dashboard - visible to all authenticated users *@
                <AuthorizeView>
                    <Authorized>
                        <NavLink href="" Match="NavLinkMatch.All"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>📊</span>
                            <span>Dashboard</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Apply for Loan - Borrower *@
                <AuthorizeView Roles="Borrower">
                    <Authorized>
                        <NavLink href="wizard"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>🚀</span>
                            <span>Apply for Loan</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* My Applications - Borrower *@
                <AuthorizeView Roles="Borrower">
                    <Authorized>
                        <NavLink href="borrower/dashboard"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>📋</span>
                            <span>My Applications</span>
                        </NavLink>
                        <NavLink href="borrower-dashboard"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>💳</span>
                            <span>My Loans</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Lender section *@
                <AuthorizeView Roles="Lender,Admin">
                    <Authorized>
                        <NavLink href="funding-queue"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>📋</span>
                            <span>Funding Queue</span>
                        </NavLink>
                        <NavLink href="lender-dashboard"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>📊</span>
                            <span>My Portfolio</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Review Queue - CrmManager, Admin *@
                <AuthorizeView Roles="Admin,CrmManager">
                    <Authorized>
                        <NavLink href="review-queue"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>🔍</span>
                            <span>Review Queue</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Loan Applications - Admin, CrmManager, Lender, Borrower *@
                <AuthorizeView Roles="Admin,CrmManager,Lender,Borrower">
                    <Authorized>
                        <NavLink href="loan-applications"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>📝</span>
                            <span>Loan Applications</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Loan Products - Admin, CrmManager, Lender *@
                <AuthorizeView Roles="Admin,CrmManager,Lender">
                    <Authorized>
                        <NavLink href="loan-products"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>🏦</span>
                            <span>Loan Products</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Borrowers - Admin, CrmManager *@
                <AuthorizeView Roles="Admin,CrmManager">
                    <Authorized>
                        <NavLink href="borrowers"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>👤</span>
                            <span>Borrowers</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Lenders - Admin *@
                <AuthorizeView Roles="Admin">
                    <Authorized>
                        <NavLink href="lenders"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>💼</span>
                            <span>Lenders</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Vetting Queue - CrmManager *@
                <AuthorizeView Roles="CrmManager">
                    <Authorized>
                        <NavLink href="vetting"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>✅</span>
                            <span>Vetting Queue</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Payments - Borrower *@
                <AuthorizeView Roles="Admin,Borrower">
                    <Authorized>
                        <NavLink href="payments"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>💳</span>
                            <span>Payments</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Disputes - CustomerService, Admin *@
                <AuthorizeView Roles="Admin,CustomerService">
                    <Authorized>
                        <NavLink href="disputes"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>⚖️</span>
                            <span>Disputes</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Messages - CustomerService, Admin *@
                <AuthorizeView Roles="Admin,CustomerService">
                    <Authorized>
                        <NavLink href="messages"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>💬</span>
                            <span>Messages</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Notifications - all authenticated *@
                <AuthorizeView>
                    <Authorized>
                        <NavLink href="notifications"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>🔔</span>
                            <span>Notifications</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Audit Logs - Admin, Auditor *@
                <AuthorizeView Roles="Admin,Auditor">
                    <Authorized>
                        <NavLink href="audit-logs"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>📋</span>
                            <span>Audit Logs</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Admin section divider *@
                <AuthorizeView Roles="Admin">
                    <Authorized>
                        <div class="pt-4 pb-2 px-4">
                            <p class="text-xs font-semibold uppercase tracking-wider text-blue-300/60">Administration</p>
                        </div>

                        <NavLink href="admin/users"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>👥</span>
                            <span>User Management</span>
                        </NavLink>

                        <NavLink href="admin/roles"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>🛡️</span>
                            <span>Roles</span>
                        </NavLink>

                        <NavLink href="admin/loans"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>🏦</span>
                            <span>All Loans</span>
                        </NavLink>

                        <NavLink href="admin/collections"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>⚖️</span>
                            <span>Collections</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>

                @* Sessions - all authenticated *@
                <AuthorizeView>
                    <Authorized>
                        <NavLink href="account/profile"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>👤</span>
                            <span>Profile</span>
                        </NavLink>

                        <NavLink href="account/notifications"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>🔔</span>
                            <span>Notifications</span>
                        </NavLink>

                        <NavLink href="help"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>❓</span>
                            <span>Help & FAQ</span>
                        </NavLink>

                        <NavLink href="account/sessions"
                                 class="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-blue-100 hover:bg-white/10"
                                 activeClass="bg-[#2563EB] text-white shadow-lg shadow-blue-900/30">
                            <span>🖥️</span>
                            <span>Sessions</span>
                        </NavLink>
                    </Authorized>
                </AuthorizeView>
            </nav>

            <div class="p-4 border-t border-white/10">
                <div class="rounded-2xl bg-white/10 p-4">
                    <div class="text-sm font-semibold">Portfolio Build</div>
                    <div class="text-xs text-blue-100 mt-1">
                        Clean Architecture + Blazor WASM
                    </div>
                </div>
            </div>
        </aside>

        <div class="flex min-w-0 flex-1 flex-col">
            <header class="h-20 border-b border-slate-200 bg-white px-6 flex items-center justify-between">
                <div>
                    <h1 class="text-xl font-semibold text-slate-900">
                        Enterprise Loan Marketplace
                    </h1>
                    <p class="text-sm text-slate-500">
                        Borrowers, lenders, products, payments and operations
                    </p>
                </div>

                <div class="flex items-center gap-4">
                    <ThemeToggle />

                    <button class="btn btn-ghost btn-circle">
                        🔔
                    </button>

                    <AuthorizeView>
                        <Authorized>
                            <div class="flex items-center gap-3">
                                <div class="avatar placeholder">
                                    <div class="bg-primary text-primary-content rounded-full w-10">
                                        <span>@GetUserInitials(context)</span>
                                    </div>
                                </div>

                                <div class="hidden sm:block">
                                    <div class="text-sm font-semibold text-slate-800">@GetUserDisplayName(context)</div>
                                    <div class="text-xs text-slate-500">@GetUserRole(context)</div>
                                </div>

                                <button @onclick="HandleLogoutAsync"
                                        class="ml-2 rounded-lg border border-slate-300 px-3 py-1.5 text-xs font-medium text-slate-600 hover:bg-slate-50"
                                        title="Sign out">
                                    Logout
                                </button>
                            </div>
                        </Authorized>
                        <NotAuthorized>
                            <a href="/login"
                               class="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">
                                Sign In
                            </a>
                        </NotAuthorized>
                    </AuthorizeView>
                </div>
            </header>

            <main class="flex-1 p-6 lg:p-8">
                @Body
            </main>
            <ToastContainer />
            <ModalHost />
            <DrawerHost />
        </div>
    </div>
</div>

@code {
    private string GetUserInitials(AuthenticationState context)
    {
        var givenName = context.User.FindFirst(System.Security.Claims.ClaimTypes.GivenName)?.Value ?? "";
        var surname = context.User.FindFirst(System.Security.Claims.ClaimTypes.Surname)?.Value ?? "";

        if (!string.IsNullOrEmpty(givenName) && !string.IsNullOrEmpty(surname))
        {
            return $"{givenName[0]}{surname[0]}".ToUpper();
        }

        var email = context.User.FindFirst(System.Security.Claims.ClaimTypes.Email)?.Value ?? "";
        return email.Length >= 2 ? email[..2].ToUpper() : "U";
    }

    private string GetUserDisplayName(AuthenticationState context)
    {
        var givenName = context.User.FindFirst(System.Security.Claims.ClaimTypes.GivenName)?.Value ?? "";
        var surname = context.User.FindFirst(System.Security.Claims.ClaimTypes.Surname)?.Value ?? "";

        if (!string.IsNullOrEmpty(givenName))
        {
            return string.IsNullOrEmpty(surname) ? givenName : $"{givenName} {surname}";
        }

        return context.User.FindFirst(System.Security.Claims.ClaimTypes.Email)?.Value ?? "User";
    }

    private string GetUserRole(AuthenticationState context)
    {
        var roles = context.User.FindAll(System.Security.Claims.ClaimTypes.Role)
            .Select(c => c.Value)
            .ToList();

        return roles.Count > 0 ? string.Join(", ", roles) : "Authenticated";
    }

    private async Task HandleLogoutAsync()
    {
        await LoanHubClient.StopAsync();
        await AuthApiClient.LogoutAsync();
        NavigationManager.NavigateTo("/login", forceLoad: false);
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            var token = await AuthProvider.GetAccessTokenAsync();
            if (!string.IsNullOrWhiteSpace(token))
            {
                await LoanHubClient.StartAsync(token);
            }
        }
    }
}
